interface:
  display_name: "OpenSpec Archive"
  short_description: "Archive completed OpenSpec changes"
  default_prompt: "Use $openspec-archive to archive a deployed or completed OpenSpec change in this repository."
