interface:
  display_name: "OpenSpec Workflow (Legacy)"
  short_description: "Compatibility shim for the old OpenSpec skill"
  default_prompt: "Use $openspec-workflow only for legacy prompts, then route to openspec-proposal, openspec-apply, or openspec-archive."
